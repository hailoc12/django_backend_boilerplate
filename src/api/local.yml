version: '3'

volumes:
  # bot_xsmb_local_postgres_data: {}
  # bot_xsmb_local_postgres_data_backups: {}
  production_postgres_data: {}
  production_postgres_data_backups: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: bot_xsmb_local_django
    container_name: bot_xsmb_local_django
    platform: linux/x86_64
    depends_on:
      - postgres
      - redis
    volumes:
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - "8000:8000"
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: bot_xsmb_production_postgres
    container_name: bot_xsmb_local_postgres
    volumes:
      # - bot_xsmb_local_postgres_data:/var/lib/postgresql/data:Z
      # - bot_xsmb_local_postgres_data_backups:/backups:z
      - production_postgres_data:/var/lib/postgresql/data:Z
      - production_postgres_data_backups:/backups:z
    env_file:
      - ./.envs/.production/.postgres
      # - ./.envs/.local/.postgres

  docs:
    image: bot_xsmb_local_docs
    container_name: bot_xsmb_local_docs
    platform: linux/x86_64
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./bot_xsmb:/app/bot_xsmb:z
    ports:
      - "9000:9000"
    command: /start-docs

  redis:
    image: redis:6
    container_name: bot_xsmb_local_redis

  celeryworker:
    <<: *django
    image: bot_xsmb_local_celeryworker
    container_name: bot_xsmb_local_celeryworker
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: bot_xsmb_local_celerybeat
    container_name: bot_xsmb_local_celerybeat
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: bot_xsmb_local_flower
    container_name: bot_xsmb_local_flower
    ports:
      - "5555:5555"
    command: /start-flower
